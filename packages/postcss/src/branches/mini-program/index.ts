import type { Result } from 'postcss'
import type { IStyleHandlerOptions } from '../../types'

export function postprocessMiniProgramCss(result: Result, _options: IStyleHandlerOptions) {
  return result
}
