import { createTailwindMerge, extendTailwindMerge, fromTheme, getDefaultConfig, mergeConfigs, twJoin, twMerge, validators } from '@/index'
import { replaceJs } from 'weapp-tailwindcss/replace'

export {
  createTailwindMerge,
  extendTailwindMerge,
  fromTheme,
  getDefaultConfig,
  mergeConfigs,
  replaceJs,
  twJoin,
  twMerge,
  validators,
}
