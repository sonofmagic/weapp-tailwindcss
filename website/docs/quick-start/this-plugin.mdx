---
title: 2. 安装 weapp-tailwindcss
description: '安装 weapp-tailwindcss，并按项目链路决定是否手动执行补丁命令。'
keywords:
  - 快速开始
  - 安装
  - 配置
  - '2.'
  - weapp-tailwindcss
  - quick start
  - this plugin
  - tailwindcss
  - 小程序
  - 微信小程序
  - uni-app
  - taro
  - rax
  - mpx
---
# 2. 安装 weapp-tailwindcss

在项目目录下，执行:

```bash npm2yarn
npm i -D weapp-tailwindcss

# 仅旧 CSS 后处理链路或排障时需要手动执行
# npx weapp-tw patch
```

如果项目使用 `weapp-tailwindcss@5` 生成模式，Tailwind CSS 会由 `WeappTailwindcss` 插件或生成器入口接管，不需要再把 `weapp-tw patch` 写入 `postinstall`。

:::info
旧 CSS 后处理链路中，手动执行 `weapp-tw patch` 主要是做2件事情

### 1. 给当前你本地的 `tailwindcss` 打上支持 `rpx` 的补丁 (小程序特有单位，非 `web` 标准)。

否则你一旦使用像 `text-[14.43rpx]` 这样的任意值写法，生成出来的 `css` 样式就是 `color: 14.43rpx;`，显然是错误的。

它会被 `tailwindcss` 认为是一种颜色，从而导致生成错误，样式不生效。

详见 [rpx 任意值颜色或长度单位二义性与解决方案](/docs/issues/rpx-ambiguities)

### 2. 暴露 `tailwindcss` 运行上下文给 `webpack`/`vite`/`glup` 插件。

这样就能够在 `js` 中，和 `postcss` 插件进行通信，从而达到共享上下文的效果。

---

如果你希望在补丁前“强制刷新 `tailwindcss-patch` 的缓存目录（如 `node_modules/.cache/tailwindcss-patch`）”，可以手动执行：

```bash npm2yarn
npx weapp-tw patch --clear-cache
```

默认不清理缓存，更加保守稳定；仅当怀疑缓存导致补丁未生效或版本不一致时再开启该参数。
:::

我们已经完成了这些步骤了，最后就是注册这个插件，到各个不同的框架里去，马上就好！
