---
{}
---
# 2. Install this plugin and run it

## Install plugin

In the project directory, execute:

```bash npm2yarn
npm i -D weapp-tailwindcss @weapp-tailwindcss/cli
```

This way the `weapp-tailwindcss` and `cli` tools will be installed on your local machine

## Execute initialization command

Run from command line

```sh
npx weapp-tw init
```

Initialize `weapp-tailwindcss` for existing native applet projects

After execution, you will find that there are three main file changes. `CLI` mainly does `3` things:

- Create `weapp-tw.config.js` file, this is the configuration file of `@weapp-tailwindcss/cli`
- Modify `package.json`, add `dev` and `build` development and build scripts, and `postinstall`
- Modify the content of `project.config.json` to adapt to the built product

## Start running

Use `npm run dev` to enter development mode. This mode comes with hot updates and is mainly used for development.

Build using `npm run build`

Whether it is `npm run dev` or `npm run build`, their build products are in the `dist` directory under the project directory

Use WeChat developer tools to directly import the project directory and then preview the effect!

>Be careful not to import the `dist` directory, but the root directory of your project! It is usually the parent directory of `dist`, don’t make a mistake!

## Configured template

If your configuration is unsuccessful, you can refer to the following template to compare configuration files:

[native-weapp-tailwindcss-template](https://github.com/sonofmagic/native-weapp-tailwindcss-template)

## More scenes

For more scenarios and configurations, please view the `More scenes` chapter;

