---
title: uni-app cli vue3 vite
description: Then run npm run dev:mp-weixin directly,
keywords:
  - quick start
  - Install
  - Configuration
  - uni-app
  - cli
  - vue3
  - vite
  - quick start
  - v4
  - uni app vite
  - weapp-tailwindcss
  - tailwindcss
  - Mini program
  - WeChat applet
  - taro
---
import CssImportReminder from '@site/src/components/CssImportReminder'
import UniappCliStyle from './UniappCliStyle.mdx'

# uni-app cli vue3 vite

:::tip
Mainline access documentation has been merged into [uni-app CLI Vue3 Vite](/docs/quick-start/frameworks/uni-app-vite). This page is reserved for legacy links and Tailwind CSS 4-specific troubleshooting.
:::

## 1. Installation

```bash npm2yarn
npm install -D tailwindcss weapp-tailwindcss
```

## 2. Configure `vite.config.ts`

```ts title="vite.config.ts"
import { dirname, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'
import { defineConfig } from "vite";
import uni from "@dcloudio/vite-plugin-uni";
import { WeappTailwindcss } from 'weapp-tailwindcss/vite'

const projectRoot = dirname(fileURLToPath(import.meta.url))

export default defineConfig({
  plugins: [
    uni(),
    WeappTailwindcss({
      cssEntries: [
        resolve(projectRoot, 'src/app.css'),
      ],
      cssOptions: {
        rem2rpx: true,
      },
    }),
  ],
});

```

> Tailwind CSS 4 projects should explicitly configure `cssEntries` and use the absolute path resolved from the project root to point to the pure `.css` entry.
> `cssEntries` is only responsible for allowing `WeappTailwindcss` to stably read the `@source` / `@config` / Tailwind instructions in the entry. The entry CSS still needs to be actually introduced into the project through `App.vue`,
> It is recommended that the Tailwind CSS 4 entry be placed in a pure `.css` file. Do not directly write it into preprocessed style files such as `scss` and `less`.

## 3. Add style

<UniappCliStyle/>

Then run `npm run dev:mp-weixin` directly,

Import this project into WeChat developer tools and you can see the effect

## Reference template

[uni-app-tailwindcss-v4](https://github.com/icebreaker-template/uni-app-tailwindcss-v4) 
