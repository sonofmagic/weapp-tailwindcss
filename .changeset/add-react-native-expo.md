---
"@weapp-tailwindcss/react-native": minor
---

新增面向 React Native 与 Expo 的 Tailwind CSS v4 原生样式编译器、Babel 转换器、Metro 集成和运行时 helper。Expo Metro 自动生成 manifest/classSet，静态 className 使用 StyleSheet lookup，动态 className 使用带缓存的 tw()，并支持 source order、`!important`、平台/深色变体和 `env` 类型入口。
